## Context

<!-- Current state and constraints that shape the approach. See proposal.md for motivation - don't restate it -->

## Goals / Non-Goals

**Goals:**

<!-- What this design aims to achieve -->

**Non-Goals:**

<!-- What is explicitly out of scope -->

## File Structure

<!--
Conditional section for frontend changes only.
Keep this section when the change affects routes, pages, layouts, providers,
components, hooks, stores, services, styles, UI dependencies, or files under src/.
Omit it for changes without frontend impact.

Show only affected files using exact repository paths. Mark every file with:
[NEW], [MODIFY], [DELETE], or [MOVE]. Include tests, styles, configuration, and
dependency files when applicable. The tree must match tasks.md.
-->

```text
<repository-root>/
├── src/
│   └── <affected-path>/
│       └── <file>.<ext>  [NEW|MODIFY|DELETE|MOVE]
└── <other-affected-file>  [NEW|MODIFY|DELETE|MOVE]
```

### Frontend Impact

<!--
Include only for frontend changes. Cover:
- affected route or user flow
- UI composition and reused/new components
- remote and local state
- loading, error, empty, and success states when data is involved
- responsive behavior
- accessibility and keyboard/focus behavior
- visual theme and design tokens
- visual, interaction, lint, and build verification
-->

## Decisions

<!-- Key design decisions with rationale and alternatives considered -->

## Risks / Trade-offs

<!-- Known risks and trade-offs -->
